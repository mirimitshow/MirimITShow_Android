<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="s2017s40.kr.hs.mirim.mirimitshow">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.ViewPaperActivity" />
        <activity android:name=".Activities.ViewBoardActivity" />
        <activity
            android:name=".Activities.AddBoardActivity"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true" />
        <activity android:name=".Activities.EnterGroupActivity" />
        <activity android:name="com.scanlibrary.ScanActivity" />
        <activity android:name=".Activities.ScanClassActivity" />
        <activity android:name=".Activities.MyPaperListActivity" />
        <activity android:name=".Activities.AddGroupActivity" />
        <activity android:name=".Activities.ScheduleActivity" />
        <activity android:name=".Activities.SignupActivity" />
        <activity android:name=".Activities.MainActivity" />
        <activity
            android:name=".Activities.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>